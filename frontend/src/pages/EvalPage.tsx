/**
 * 占位 — Phase 6 接入 eval runner 触发 / Langfuse score 查看。
 */
export default function EvalPage() {
  return (
    <div className="max-w-4xl mx-auto p-8">
      <h1 className="text-2xl font-semibold mb-4">Eval</h1>
      <p className="text-gray-600">Phase 6 接入 — ground truth 跑批 + Langfuse scores。</p>
    </div>
  );
}